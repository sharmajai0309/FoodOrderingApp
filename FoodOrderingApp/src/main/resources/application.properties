spring.application.name=FoodOrderingApp

#Server Configuration
server.port=5454


spring.profiles.active=dev


# ── Kafka Producer Config ────────────────────────────────────────────
# Broker from docker-compose: delivery-kafka on localhost:9092
spring.kafka.bootstrap-servers=localhost:9092

# Serialize key as String (we use orderId.toString() as key)
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer

# Serialize value as JSON (our OrderConfirmedEvent POJO)
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Don't add Spring type headers — keeps consumer decoupled (no class name in header)
spring.kafka.producer.properties.spring.json.add.type.headers=false

# Producer reliability: wait for leader acknowledgement (good for small biz)
spring.kafka.producer.acks=1

# ── Kafka Consumer Config (food-service listening to delivery events) ──
spring.kafka.consumer.group-id=food-service-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*
spring.kafka.consumer.properties.spring.json.use.type.headers=false
